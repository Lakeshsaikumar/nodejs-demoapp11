trigger:
- main   # auto-build on main branch

stages:

# =========================
# Stage 1: Build
# =========================
- stage: Build
  displayName: 'Build Node.js App'
  jobs:
  - job: BuildJob
    displayName: 'Build Node Project'
    pool:
      vmImage: 'ubuntu-latest'

    steps:
    - checkout: self
      clean: true

    - task: NodeTool@0
      inputs:
        versionSpec: '18.x'
      displayName: 'Install Node.js 18'

    - script: |
        echo "Root folder:"
        ls -la
        echo "src folder:"
        ls -la src
      displayName: 'Debug: List Files'

    - script: npm install
      workingDirectory: '$(Build.SourcesDirectory)/src'
      displayName: 'Install Dependencies'

    - script: npm run build --if-present
      workingDirectory: '$(Build.SourcesDirectory)/src'
      displayName: 'Run Build Script'

    - task: PublishPipelineArtifact@1
      inputs:
        targetPath: '$(Build.SourcesDirectory)/src'
        artifactName: 'nodeapp'
      displayName: 'Publish Build Artifact'

# =========================
# Stage 2: Deploy
# =========================
- stage: Deploy
  displayName: 'Deploy Node App'
  dependsOn: Build
  jobs:
  - job: DeployJob
    displayName: 'Deploy to Remote Server'
    pool:
      name: 'Java_Deployment'

    steps:
    - download: current
      artifact: 'nodeapp'

    - task: CopyFilesOverSSH@0
      displayName: 'Copy Files to Server'
      inputs:
        sshEndpoint: 'Java_YAML'
        sourceFolder: '$(Pipeline.Workspace)/nodeapp'
        contents: '**/*'
        targetFolder: '/home/itadmin/nodeapp'

    - task: SSH@0
      displayName: 'Install & Restart Node.js App (port 3000)'
      inputs:
        sshEndpoint: 'Java_YAML'
        runOptions: 'inline'
        inline: |
          #!/bin/bash
          APP_DIR="/home/itadmin/nodeapp"
          APP_FILE="server.mjs"
          LOG_FILE="server.log"

          # Navigate to app directory
          cd "$APP_DIR" || { echo "App directory not found!"; exit 1; }

          # Stop any running app
          echo "Stopping existing Node.js app..."
          pkill -f "$APP_FILE" || echo "No running process found."

          # Install dependencies
          echo "Installing dependencies..."
          npm install

          # Start the app in background (port 3000)
          echo "Starting Node.js app in background (port 3000)..."
          nohup node "$APP_FILE" > "$LOG_FILE" 2>&1 &

          # Verify
          echo "App started. Logging to $LOG_FILE"
          ps -ef | grep "$APP_FILE" | grep -v grep

